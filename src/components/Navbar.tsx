const Navbar = () => (
  <nav className="w-full py-4 px-6 bg-primary text-primary-foreground shadow flex items-center justify-between">
    <h1 className="text-xl font-bold tracking-tight">Code Review Interview Tool</h1>
  </nav>
);

export default Navbar;
